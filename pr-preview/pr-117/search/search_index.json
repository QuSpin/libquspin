{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"QuSpin-rs","text":"<p>Rust rewrite of the QuSpin quantum many-body library, with Python bindings via PyO3 / maturin.</p> <p>This site documents the Python package <code>quspin_rs</code> exported from the <code>quspin-rs</code> project.</p>"},{"location":"#install","title":"Install","text":"<pre><code>just sync       # uv sync --dev --all-extras\njust develop    # uv run maturin develop  (off-by-default: large-int)\n</code></pre> <p>For wide-integer (\u2265512-bit) bases, build with the feature on:</p> <pre><code>just develop --features large-int\n</code></pre>"},{"location":"#quick-tour","title":"Quick tour","text":"<pre><code>import numpy as np\nfrom quspin_rs import (\n    Lattice, Local, SpinBasis, SymmetryGroup, PauliOperator, QMatrix,\n)\nfrom quspin_rs._rs import ExpmOp, Hamiltonian, Static\n\nn_sites = 4\nH = PauliOperator([(\"XX\", [[1.0, i, (i + 1) % n_sites] for i in range(n_sites)])])\n\n# Translation \u00d7 spin-flip symmetric basis (k=0 momentum, eta=-1 parity sector).\nT = SymmetryGroup(n_sites=n_sites, lhss=2)\nT.add_cyclic(Lattice([(i + 1) % n_sites for i in range(n_sites)]), k=0)\nZ = SymmetryGroup(n_sites=n_sites, lhss=2)\nZ.add_cyclic(Local([1, 0]), eta=-1)\ngroup = T.product(Z)\n\nbasis = SpinBasis.symmetric(group, H, seeds=[\"0011\"])\nprint(basis.size)\n\nmat = QMatrix.build_pauli(H, basis, np.dtype(\"float64\"))\n\n# Time-evolve a state under H: psi \u2190 exp(-i\u00b7dt\u00b7H) \u00b7 psi\nham = Hamiltonian(mat, [Static()])\nexpm_op = ExpmOp(ham.as_linearoperator(0.0), a=-1j * 0.05)\nworker = expm_op.worker()              # 1-D worker (n_vec=0); 2*dim scratch reused\npsi = np.zeros(basis.size, dtype=np.complex128)\npsi[0] = 1.0\nworker.apply(psi)                      # one timestep, no allocations\n</code></pre>"},{"location":"#where-to-start","title":"Where to start","text":"<ul> <li>Symmetry-group guide \u2014 how to build groups for   <code>*Basis.symmetric(...)</code>.</li> <li>Distributed assembly with petsc4py \u2014 building   PETSc matrices row-by-row across MPI ranks via <code>op.csr_slab(...)</code>.</li> <li>Basis types \u2014 <code>SpinBasis</code>, <code>FermionBasis</code>,   <code>BosonBasis</code>, <code>GenericBasis</code>.</li> <li>Symmetry API \u2014 <code>SymmetryGroup</code> plus the   <code>Lattice</code> / <code>Local</code> / <code>Composite</code> element constructors.</li> <li>Operators \u2014 <code>PauliOperator</code>, <code>BondOperator</code>,   <code>BosonOperator</code>, <code>FermionOperator</code>, <code>MonomialOperator</code>.</li> <li>QMatrix &amp; Hamiltonian \u2014 sparse matrix construction   and time-evolution.</li> </ul>"},{"location":"#a-working-demo","title":"A working demo","text":"<p><code>scripts/symmetry_group_demo.py</code> walks through five canonical patterns (translation only, abelian product, PZ composite, dihedral closure, and a compatibility-error case). Run it with:</p> <pre><code>uv run python scripts/symmetry_group_demo.py\n</code></pre>"},{"location":"api/basis/","title":"Basis types","text":"<p>Four user-facing basis wrappers, each with three constructors:</p> <ul> <li><code>full(...)</code> \u2014 full Hilbert space, no projection.</li> <li><code>subspace(...)</code> \u2014 particle-number / energy sector built by BFS.</li> <li><code>symmetric(group, ham, seeds)</code> \u2014 symmetry-projected subspace.</li> </ul> <p><code>n_sites</code> and <code>lhss</code> come from the <code>SymmetryGroup</code> for the symmetric constructor; the full / subspace constructors take them positionally.</p>"},{"location":"api/basis/#spinbasis","title":"SpinBasis","text":""},{"location":"api/basis/#quspin_rs._rs.SpinBasis","title":"SpinBasis","text":"<p>Spin (or hardcore-boson) basis \u2014 LHSS = 2 by default.</p> <p>Supports full (all states), subspace (reachable from seeds under a Hamiltonian), and symmetric (symmetry-projected) construction.</p>"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.full","title":"full  <code>classmethod</code>","text":"<pre><code>full(n_sites: int, lhss: int = 2) -&gt; SpinBasis\n</code></pre> <p>Full Hilbert space (no restriction).</p>"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.subspace","title":"subspace  <code>classmethod</code>","text":"<pre><code>subspace(\n    n_sites: int,\n    ham: PauliOperator | BondOperator,\n    seeds: list[str],\n    lhss: int = 2,\n) -&gt; SpinBasis\n</code></pre> <p>Krylov subspace spanned by acting <code>ham</code> on <code>seeds</code>.</p> <p>Parameters:</p> Name Type Description Default <code>seeds</code> <code>list[str]</code> <p>List of bit-strings, e.g. <code>[\"0101\", \"1010\"]</code>.</p> required"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.symmetric","title":"symmetric  <code>classmethod</code>","text":"<pre><code>symmetric(\n    group: Any,\n    ham: PauliOperator | BondOperator,\n    seeds: list[str],\n) -&gt; SpinBasis\n</code></pre> <p>Symmetry-projected subspace.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>Any</code> <p>a <code>SymmetryGroup</code>; <code>n_sites</code> and <code>lhss</code> are read from <code>group.n_sites</code> / <code>group.lhss</code>.</p> required <code>ham</code> <code>PauliOperator | BondOperator</code> <p><code>PauliOperator</code> (LHSS=2) or <code>BondOperator</code> for BFS.</p> required <code>seeds</code> <code>list[str]</code> <p>list of seed state strings.</p> required"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.state_at","title":"state_at","text":"<pre><code>state_at(i: int) -&gt; str\n</code></pre> <p>Return the i-th basis state as a bit-string.</p>"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.index_str","title":"index_str","text":"<pre><code>index_str(state: str) -&gt; int | None\n</code></pre> <p>Return the index of a state given as a bit-string, or <code>None</code> if absent.</p>"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.index","title":"index","text":"<pre><code>index(state: int) -&gt; int | None\n</code></pre> <p>Return the index of an integer-encoded state, or <code>None</code> if absent.</p>"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.project_to","title":"project_to","text":"<pre><code>project_to(\n    state: (\n        NDArray[Any] | Sequence[complex] | Sequence[float]\n    ),\n    sparse: bool = True,\n) -&gt; npt.NDArray[np.complex128]\n</code></pre> <p>Project a full-basis state vector into this basis.</p>"},{"location":"api/basis/#quspin_rs._rs.SpinBasis.project_from","title":"project_from","text":"<pre><code>project_from(\n    state: (\n        NDArray[Any] | Sequence[complex] | Sequence[float]\n    ),\n    sparse: bool = True,\n) -&gt; npt.NDArray[np.complex128]\n</code></pre> <p>Expand a state vector in this basis to the full basis.</p>"},{"location":"api/basis/#fermionbasis","title":"FermionBasis","text":""},{"location":"api/basis/#quspin_rs._rs.FermionBasis","title":"FermionBasis","text":"<p>Fermionic basis \u2014 LHSS = 2.</p>"},{"location":"api/basis/#bosonbasis","title":"BosonBasis","text":""},{"location":"api/basis/#quspin_rs._rs.BosonBasis","title":"BosonBasis","text":"<p>Bosonic basis \u2014 LHSS user-supplied (&gt;= 2).</p>"},{"location":"api/basis/#genericbasis","title":"GenericBasis","text":""},{"location":"api/basis/#quspin_rs._rs.GenericBasis","title":"GenericBasis","text":"<p>Generic basis for any on-site Hilbert-space size (LHSS \u2265 2).</p> <p>Supports lattice (site-permutation) and local (dit-permutation) symmetries. Paired with <code>MonomialOperator</code> for Hamiltonian construction.</p>"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.full","title":"full  <code>classmethod</code>","text":"<pre><code>full(n_sites: int, lhss: int) -&gt; GenericBasis\n</code></pre> <p>Full Hilbert space (no restriction, no build step required).</p>"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.subspace","title":"subspace  <code>classmethod</code>","text":"<pre><code>subspace(\n    n_sites: int,\n    lhss: int,\n    ham: MonomialOperator,\n    seeds: list[str],\n) -&gt; GenericBasis\n</code></pre> <p>Subspace built by BFS from seed states.</p> <p>Parameters:</p> Name Type Description Default <code>seeds</code> <code>list[str]</code> <p>List of state strings (one digit per site, base <code>lhss</code>).</p> required"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.symmetric","title":"symmetric  <code>classmethod</code>","text":"<pre><code>symmetric(\n    group: Any, ham: MonomialOperator, seeds: list[str]\n) -&gt; GenericBasis\n</code></pre> <p>Symmetry-reduced subspace.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>Any</code> <p>a <code>SymmetryGroup</code>; <code>n_sites</code> and <code>lhss</code> are read from <code>group.n_sites</code> / <code>group.lhss</code>.</p> required <code>ham</code> <code>MonomialOperator</code> <p><code>MonomialOperator</code> for BFS.</p> required <code>seeds</code> <code>list[str]</code> <p>list of seed state strings.</p> required"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.state_at","title":"state_at","text":"<pre><code>state_at(i: int) -&gt; str\n</code></pre> <p>Return the i-th basis state as a string of site occupations.</p>"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.index_str","title":"index_str","text":"<pre><code>index_str(state: str) -&gt; int | None\n</code></pre> <p>Return the index of a state string, or <code>None</code> if absent.</p>"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.index","title":"index","text":"<pre><code>index(state: int) -&gt; int | None\n</code></pre> <p>Return the index of an integer-encoded state, or <code>None</code> if absent.</p>"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.project_to","title":"project_to","text":"<pre><code>project_to(\n    state: (\n        NDArray[Any] | Sequence[complex] | Sequence[float]\n    ),\n    sparse: bool = True,\n) -&gt; npt.NDArray[np.complex128]\n</code></pre> <p>Project a full-basis state vector into this basis.</p>"},{"location":"api/basis/#quspin_rs._rs.GenericBasis.project_from","title":"project_from","text":"<pre><code>project_from(\n    state: (\n        NDArray[Any] | Sequence[complex] | Sequence[float]\n    ),\n    sparse: bool = True,\n) -&gt; npt.NDArray[np.complex128]\n</code></pre> <p>Expand a state vector in this basis to the full basis.</p>"},{"location":"api/operators/","title":"Operators","text":"<p>Operator types feed the BFS that builds subspace / symmetric bases, and are also the source of matrix elements when assembled into a <code>QMatrix</code>.</p>"},{"location":"api/operators/#paulioperator","title":"PauliOperator","text":""},{"location":"api/operators/#quspin_rs._rs.PauliOperator","title":"PauliOperator","text":"<pre><code>PauliOperator(*terms: list[tuple[str, list[list[Any]]]])\n</code></pre> <p>Pauli / hardcore-boson operator.</p> <p>Each positional argument is a term (one coupling-constant index). A term is a list of <code>(op_str, bonds)</code> pairs that share that cindex. Each bond is <code>[coeff, site0, site1, ...]</code>.</p> <p>Example::</p> <pre><code>bonds = [[1.0, 0, 1], [1.0, 1, 2], [1.0, 2, 3]]\n# Two cindices (XX and ZZ can have independent coefficients):\nop = PauliOperator([(\"XX\", bonds)], [(\"ZZ\", bonds)])\n# One cindex (XX and ZZ always share the same coefficient):\nop = PauliOperator([(\"XX\", bonds), (\"ZZ\", bonds)])\n</code></pre>"},{"location":"api/operators/#quspin_rs._rs.PauliOperator.apply_and_project_to","title":"apply_and_project_to","text":"<pre><code>apply_and_project_to(\n    input_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    output_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code> in <code>input_basis</code>, project into <code>output_basis</code>.</p>"},{"location":"api/operators/#quspin_rs._rs.PauliOperator.apply","title":"apply","text":"<pre><code>apply(\n    basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code>, projecting back into the same <code>basis</code>.</p>"},{"location":"api/operators/#quspin_rs._rs.PauliOperator.csr_slab","title":"csr_slab","text":"<pre><code>csr_slab(\n    basis: SpinBasis | FermionBasis,\n    coeffs: NDArray[Any],\n    row_start: int,\n    row_end: int,\n    dtype: dtype[Any],\n    drop_zeros: bool = True,\n) -&gt; tuple[\n    npt.NDArray[np.int64],\n    npt.NDArray[np.int64],\n    npt.NDArray[Any],\n]\n</code></pre> <p>Materialise rows <code>[row_start, row_end)</code> as CSR.</p> <p>Designed for petsc4py: each MPI rank calls this with its locally-owned row range.  Returns <code>(indptr, indices, data)</code> where <code>indices</code> are global column indices.  See the petsc4py demo at <code>examples/petsc4py_chunked_build.py</code>.</p>"},{"location":"api/operators/#bondoperator","title":"BondOperator","text":""},{"location":"api/operators/#quspin_rs._rs.BondOperator","title":"BondOperator","text":"<pre><code>BondOperator(\n    *terms: list[tuple[NDArray[Any], list[tuple[int, int]]]]\n)\n</code></pre> <p>Dense two-site bond operator.</p> <p>Variadic <code>*terms</code> \u2014 the i-th positional argument carries cindex <code>i</code>. Each term is a list of <code>(matrix, bonds)</code> pairs that share that cindex:</p> <ul> <li><code>matrix</code>: 2-D complex128 array of shape <code>(lhss**2, lhss**2)</code></li> <li><code>bonds</code>: list of <code>(site_i, site_j)</code> pairs</li> </ul> <p>Example::</p> <pre><code>op = BondOperator(\n    [(M1, [(0, 1), (1, 2)])],            # cindex 0\n    [(M2, [(0, 1)]), (M3, [(1, 2)])],    # cindex 1\n)\n</code></pre>"},{"location":"api/operators/#quspin_rs._rs.BondOperator.apply_and_project_to","title":"apply_and_project_to","text":"<pre><code>apply_and_project_to(\n    input_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    output_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code> in <code>input_basis</code>, project into <code>output_basis</code>.</p>"},{"location":"api/operators/#quspin_rs._rs.BondOperator.apply","title":"apply","text":"<pre><code>apply(\n    basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code>, projecting back into the same <code>basis</code>.</p>"},{"location":"api/operators/#bosonoperator","title":"BosonOperator","text":""},{"location":"api/operators/#quspin_rs._rs.BosonOperator","title":"BosonOperator","text":"<pre><code>BosonOperator(\n    *terms: list[tuple[str, list[list[Any]]]], lhss: int\n)\n</code></pre> <p>Bosonic operator.</p> <p>Same variadic <code>*terms</code> format as <code>PauliOperator</code>, using boson op strings (<code>+</code>, <code>-</code>, <code>n</code>).  <code>lhss</code> is keyword-only.</p>"},{"location":"api/operators/#quspin_rs._rs.BosonOperator.apply_and_project_to","title":"apply_and_project_to","text":"<pre><code>apply_and_project_to(\n    input_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    output_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code> in <code>input_basis</code>, project into <code>output_basis</code>.</p>"},{"location":"api/operators/#quspin_rs._rs.BosonOperator.apply","title":"apply","text":"<pre><code>apply(\n    basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code>, projecting back into the same <code>basis</code>.</p>"},{"location":"api/operators/#fermionoperator","title":"FermionOperator","text":""},{"location":"api/operators/#quspin_rs._rs.FermionOperator","title":"FermionOperator","text":"<pre><code>FermionOperator(*terms: list[tuple[str, list[list[Any]]]])\n</code></pre> <p>Fermionic operator.</p> <p>Same variadic <code>*terms</code> format as <code>PauliOperator</code>, using fermion op strings (<code>+</code>, <code>-</code>, <code>n</code>).  Jordan-Wigner signs are applied automatically.</p>"},{"location":"api/operators/#quspin_rs._rs.FermionOperator.apply_and_project_to","title":"apply_and_project_to","text":"<pre><code>apply_and_project_to(\n    input_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    output_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code> in <code>input_basis</code>, project into <code>output_basis</code>.</p>"},{"location":"api/operators/#quspin_rs._rs.FermionOperator.apply","title":"apply","text":"<pre><code>apply(\n    basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code>, projecting back into the same <code>basis</code>.</p>"},{"location":"api/operators/#monomialoperator","title":"MonomialOperator","text":""},{"location":"api/operators/#quspin_rs._rs.MonomialOperator","title":"MonomialOperator","text":"<pre><code>MonomialOperator(\n    *terms: tuple[\n        NDArray[intp],\n        NDArray[complexfloating[Any, Any]],\n        list[tuple[int, ...]],\n    ],\n    lhss: int\n)\n</code></pre> <p>Generic monomial-matrix operator (one non-zero per row).</p> <p>Each positional term argument is a 3-tuple <code>(perm, amp, bonds)</code> where:</p> <ul> <li><code>perm</code>: 1-D integer array of length <code>lhss**k</code> \u2014 output joint-state   index for each input joint-state index.</li> <li><code>amp</code>:  1-D complex128 array of length <code>lhss**k</code> \u2014 complex amplitude   for each input joint-state.</li> <li><code>bonds</code>: list of k-tuples of site indices; all bonds in one term must   have the same number of sites <code>k</code>.</li> </ul> <p>Cindex (coupling-constant index) is implicit by position: the i-th term gets cindex <code>i</code>.  <code>lhss</code> is keyword-only (matching <code>BosonOperator</code>).</p> <p>Example (cyclic shift on nearest-neighbour bonds, lhss=3, 4 sites)::</p> <pre><code>import numpy as np\nlhss = 3\nk = 2          # 2-site bond\ndim = lhss**k  # 9\nperm = np.array([\n    lhss * ((a + 1) % lhss) + (b + 1) % lhss\n    for a in range(lhss) for b in range(lhss)\n], dtype=np.intp)\namp = np.ones(dim, dtype=complex)\nbonds = [(0, 1), (1, 2), (2, 3)]\nop = MonomialOperator((perm, amp, bonds), lhss=lhss)\n</code></pre>"},{"location":"api/operators/#quspin_rs._rs.MonomialOperator.num_coeffs","title":"num_coeffs  <code>property</code>","text":"<pre><code>num_coeffs: int\n</code></pre> <p>Number of distinct coupling-constant indices (= number of terms).</p>"},{"location":"api/operators/#quspin_rs._rs.MonomialOperator.apply_and_project_to","title":"apply_and_project_to","text":"<pre><code>apply_and_project_to(\n    input_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    output_basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code> in <code>input_basis</code>, project into <code>output_basis</code>.</p>"},{"location":"api/operators/#quspin_rs._rs.MonomialOperator.apply","title":"apply","text":"<pre><code>apply(\n    basis: (\n        SpinBasis | FermionBasis | BosonBasis | GenericBasis\n    ),\n    coeffs: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool = True,\n) -&gt; None\n</code></pre> <p>Apply operator to <code>input</code>, projecting back into the same <code>basis</code>.</p>"},{"location":"api/qmatrix/","title":"QMatrix &amp; Hamiltonian","text":"<p>Sparse matrix construction (<code>QMatrix</code>), time-dependent Hamiltonian wrapper (<code>Hamiltonian</code> with <code>Static</code> and Python callable coefficients), matrix-exponential action (<code>ExpmOp</code> + <code>ExpmWorker</code> / <code>ExpmWorker2</code>), and the Schr\u00f6dinger-equation integrator (<code>SchrodingerEq</code>).</p>"},{"location":"api/qmatrix/#qmatrix","title":"QMatrix","text":""},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix","title":"QMatrix","text":"<p>Sparse quantum matrix built from an operator + basis pair.</p> <p>Use the <code>build_*</code> static methods to construct.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.build_pauli","title":"build_pauli  <code>staticmethod</code>","text":"<pre><code>build_pauli(\n    op: PauliOperator,\n    basis: SpinBasis | FermionBasis,\n    dtype: dtype[Any],\n) -&gt; QMatrix\n</code></pre> <p>Build from a PauliOperator and a SpinBasis or FermionBasis.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.build_bond","title":"build_bond  <code>staticmethod</code>","text":"<pre><code>build_bond(\n    op: BondOperator,\n    basis: SpinBasis | FermionBasis | BosonBasis,\n    dtype: dtype[Any],\n) -&gt; QMatrix\n</code></pre> <p>Build from a BondOperator and any basis type.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.build_boson","title":"build_boson  <code>staticmethod</code>","text":"<pre><code>build_boson(\n    op: BosonOperator, basis: BosonBasis, dtype: dtype[Any]\n) -&gt; QMatrix\n</code></pre> <p>Build from a BosonOperator and a BosonBasis.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.build_fermion","title":"build_fermion  <code>staticmethod</code>","text":"<pre><code>build_fermion(\n    op: FermionOperator,\n    basis: FermionBasis,\n    dtype: dtype[Any],\n) -&gt; QMatrix\n</code></pre> <p>Build from a FermionOperator and a FermionBasis.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.build_monomial","title":"build_monomial  <code>staticmethod</code>","text":"<pre><code>build_monomial(\n    op: MonomialOperator,\n    basis: GenericBasis,\n    dtype: dtype[Any],\n) -&gt; QMatrix\n</code></pre> <p>Build from a MonomialOperator and a GenericBasis.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.to_csr","title":"to_csr","text":"<pre><code>to_csr(\n    coeff: NDArray[Any], drop_zeros: bool = True\n) -&gt; tuple[\n    npt.NDArray[np.int64],\n    npt.NDArray[np.int64],\n    npt.NDArray[Any],\n]\n</code></pre> <p>Return <code>(indptr, indices, data)</code> CSR arrays.</p> <p>Parameters:</p> Name Type Description Default <code>coeff</code> <code>NDArray[Any]</code> <p>1-D complex128 array of length <code>num_coeff</code>.</p> required"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.dot_many","title":"dot_many","text":"<pre><code>dot_many(\n    coeff: NDArray[Any],\n    input: NDArray[Any],\n    output: NDArray[Any],\n    overwrite: bool,\n) -&gt; None\n</code></pre> <p>Batch matvec: <code>output += coeff * self @ input</code>.</p> <p>Parameters:</p> Name Type Description Default <code>coeff</code> <code>NDArray[Any]</code> <p>1-D complex128 of length <code>num_coeff</code>.</p> required <code>input</code> <code>NDArray[Any]</code> <p>2-D complex128 of shape <code>(dim, n_vecs)</code> (C-contiguous).</p> required <code>output</code> <code>NDArray[Any]</code> <p>2-D complex128 of shape <code>(dim, n_vecs)</code> (modified in place).</p> required"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrix.as_linearoperator","title":"as_linearoperator","text":"<pre><code>as_linearoperator(\n    coeffs: NDArray[Any],\n) -&gt; QMatrixLinearOperator\n</code></pre> <p>Snapshot the matrix with a fully-evaluated coefficient vector.</p> <p>Parameters:</p> Name Type Description Default <code>coeffs</code> <code>NDArray[Any]</code> <p>1-D complex128 array of length <code>num_coeff</code>.</p> required <p>Returns:</p> Type Description <code>QMatrixLinearOperator</code> <p>A <code>QMatrixLinearOperator</code> sharing this matrix's allocation.</p>"},{"location":"api/qmatrix/#hamiltonian","title":"Hamiltonian","text":""},{"location":"api/qmatrix/#quspin_rs._rs.Hamiltonian","title":"Hamiltonian","text":"<pre><code>Hamiltonian(\n    qmatrix: QMatrix,\n    coeff_fns: list[Static | Callable[[float], complex]],\n)\n</code></pre> <p>Time-dependent Hamiltonian.</p> <p>Wraps a <code>QMatrix</code> together with coefficient descriptors \u2014 one per cindex.  Each entry is either <code>Static()</code> (coefficient 1.0) or a callable <code>f(t) -&gt; complex</code>.</p> <p>Parameters:</p> Name Type Description Default <code>qmatrix</code> <code>QMatrix</code> <p>A <code>QMatrix</code> with <code>num_coeff</code> operator strings.</p> required <code>coeff_fns</code> <code>list[Static | Callable[[float], complex]]</code> <p>List of length <code>qmatrix.num_coeff</code>.  Each element is        <code>Static()</code> for a time-independent term or a callable        <code>f(t: float) -&gt; complex</code> for a time-dependent term.</p> required"},{"location":"api/qmatrix/#quspin_rs._rs.Hamiltonian.to_dense","title":"to_dense","text":"<pre><code>to_dense(\n    time: float,\n) -&gt; npt.NDArray[np.complexfloating[Any, Any]]\n</code></pre> <p>Return the Hamiltonian at <code>time</code> as a dense <code>(dim, dim)</code> complex128 matrix.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.Hamiltonian.as_linearoperator","title":"as_linearoperator","text":"<pre><code>as_linearoperator(time: float) -&gt; QMatrixLinearOperator\n</code></pre> <p>Snapshot the Hamiltonian at <code>time</code> as a <code>QMatrixLinearOperator</code>.</p> <p>Evaluates every time-dependent coefficient and clones the underlying matrix.  The returned operator can be passed to <code>ExpmOp</code>.</p>"},{"location":"api/qmatrix/#static","title":"Static","text":""},{"location":"api/qmatrix/#quspin_rs._rs.Static","title":"Static","text":"<pre><code>Static()\n</code></pre> <p>Marker indicating a static (time-independent) coefficient.</p> <p>Pass <code>Static()</code> in the <code>coeff_fns</code> list to mark a cindex as having a constant coefficient of 1.0.</p>"},{"location":"api/qmatrix/#matrix-exponential","title":"Matrix exponential","text":"<p>The matrix-exponential action <code>f \u2190 exp(a\u00b7A) \u00b7 f</code> is exposed through three cooperating types:</p> <ol> <li><code>QMatrixLinearOperator</code> \u2014 a snapshot of a <code>QMatrix</code> (or a    <code>Hamiltonian</code> evaluated at a specific <code>time</code>) paired with a fully    evaluated coefficient vector.  Construct with    <code>QMatrix.as_linearoperator(coeffs)</code> or    <code>Hamiltonian.as_linearoperator(time)</code>.</li> <li><code>ExpmOp</code> \u2014 caches the partitioned-Taylor parameters    <code>(\u03bc, s, m_star, tol)</code> for a given <code>(qop, a)</code> pair.  Construction runs    the parameter selection exactly once.</li> <li><code>ExpmWorker</code> (1-D) / <code>ExpmWorker2</code> (2-D batch) \u2014 packages    reusable scratch memory bound to an <code>ExpmOp</code>.  Built via    <code>expm_op.worker(n_vec, work)</code>, which dispatches at construction time:    <code>n_vec == 0</code> (the default) returns <code>ExpmWorker</code> for single-vector    application; <code>n_vec &gt; 0</code> returns <code>ExpmWorker2</code> whose <code>apply</code> accepts    a <code>(dim, k)</code> array with <code>k &lt;= n_vec</code>.</li> </ol> <pre><code>import numpy as np\nfrom quspin_rs._rs import (\n    ExpmOp, Hamiltonian, PauliOperator, QMatrix, SpinBasis, Static,\n)\n\nH = PauliOperator([(\"XX\", [[1.0, 0, 1]])])\nbasis = SpinBasis.full(2)\nmat = QMatrix.build_pauli(H, basis, np.dtype(\"float64\"))\nham = Hamiltonian(mat, [Static()])\n\n# Snapshot the Hamiltonian at t=0 and cache the Taylor parameters once.\nqop = ham.as_linearoperator(0.0)\nexpm_op = ExpmOp(qop, a=-1j * 0.05)            # `a` = -i\u00b7dt for time evolution\n\n# Single-vector worker (n_vec=0, the default).\nworker = expm_op.worker()\npsi = np.array([1, 0, 0, 0], dtype=np.complex128)\nworker.apply(psi)         # psi \u2190 exp(-i\u00b7dt\u00b7H) \u00b7 psi\nworker.apply(psi)         # second step, no allocations\n\n# Batch worker \u2014 accepts a (dim, k) array with k &lt;= n_vec.\nbatch_worker = expm_op.worker(n_vec=4)\nPsi = np.eye(4, dtype=np.complex128)\nbatch_worker.apply(Psi)   # Psi \u2190 exp(-i\u00b7dt\u00b7H) \u00b7 Psi  (one column per state)\n</code></pre> <p>The <code>apply</code> methods borrow the input numpy array directly \u2014 no allocations on the hot path.  The optional <code>work=</code> argument to <code>worker(...)</code> lets the caller pre-allocate the worker's scratch buffer.</p>"},{"location":"api/qmatrix/#qmatrixlinearoperator","title":"QMatrixLinearOperator","text":""},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator","title":"QMatrixLinearOperator","text":"<p>Python-facing snapshot of a <code>QMatrix</code> + coefficient vector \u2014 i.e. a concrete <code>LinearOperator&lt;Complex&lt;f64&gt;&gt;</code>.</p> <p>Internally an <code>Arc&lt;OwnedQMatrixOperator&lt;Complex&lt;f64&gt;&gt;&gt;</code> shared with <code>PyExpmOp</code> so the same matrix allocation backs many <code>apply</code> calls. Constructed via <code>QMatrix.as_linearoperator(coeffs)</code> or <code>Hamiltonian.as_linearoperator(time)</code>; not constructible from Python directly.</p> <p>Exposes the SciPy <code>LinearOperator</code> duck-typed interface (<code>shape</code>, <code>dtype</code>, <code>matvec</code>, <code>matmat</code>, <code>rmatvec</code>, <code>rmatmat</code>, <code>@</code>) so instances can be passed directly to <code>scipy.sparse.linalg</code> routines.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.__doc__","title":"__doc__  <code>class-attribute</code>","text":"<pre><code>__doc__ = \"Python-facing snapshot of a `QMatrix` + coefficient vector \u2014 i.e. a\\nconcrete `LinearOperator&lt;Complex&lt;f64&gt;&gt;`.\\n\\nInternally an `Arc&lt;OwnedQMatrixOperator&lt;Complex&lt;f64&gt;&gt;&gt;` shared with\\n`PyExpmOp` so the same matrix allocation backs many `apply` calls.\\nConstructed via `QMatrix.as_linearoperator(coeffs)` or\\n`Hamiltonian.as_linearoperator(time)`; not constructible from Python\\ndirectly.\\n\\nExposes the SciPy `LinearOperator` duck-typed interface (`shape`,\\n`dtype`, `matvec`, `matmat`, `rmatvec`, `rmatmat`, `@`) so instances can\\nbe passed directly to `scipy.sparse.linalg` routines.\"\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.__module__","title":"__module__  <code>class-attribute</code>","text":"<pre><code>__module__ = 'quspin_rs._rs'\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.dim","title":"dim  <code>property</code>","text":"<pre><code>dim: int\n</code></pre>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.dtype","title":"dtype  <code>property</code>","text":"<pre><code>dtype: dtype[Any]\n</code></pre> <p>SciPy-compatible <code>numpy.dtype</code>.  Always <code>complex128</code> \u2014 matvec is run in complex128 regardless of the matrix's storage dtype because <code>as_linearoperator</code> accepts complex coefficients.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.num_coeff","title":"num_coeff  <code>property</code>","text":"<pre><code>num_coeff: int\n</code></pre>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>SciPy-compatible <code>(dim, dim)</code> shape tuple.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.__matmul__","title":"__matmul__  <code>method descriptor</code>","text":"<pre><code>__matmul__(\n    value,\n) -&gt; npt.NDArray[np.complexfloating[Any, Any]]\n</code></pre> <p>Return self@value.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.__repr__","title":"__repr__  <code>method descriptor</code>","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return repr(self).</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.__rmatmul__","title":"__rmatmul__  <code>method descriptor</code>","text":"<pre><code>__rmatmul__(\n    value,\n) -&gt; npt.NDArray[np.complexfloating[Any, Any]]\n</code></pre> <p>Return value@self.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.matmat","title":"matmat  <code>method descriptor</code>","text":"<pre><code>matmat(\n    x: NDArray[complexfloating[Any, Any]],\n) -&gt; npt.NDArray[np.complexfloating[Any, Any]]\n</code></pre> <p><code>A @ X</code> for a 2-D complex128 input of shape <code>(dim, k)</code>.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.matvec","title":"matvec  <code>method descriptor</code>","text":"<pre><code>matvec(\n    x: NDArray[complexfloating[Any, Any]],\n) -&gt; npt.NDArray[np.complexfloating[Any, Any]]\n</code></pre> <p><code>A @ x</code> for a 1-D complex128 input.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.rmatmat","title":"rmatmat  <code>method descriptor</code>","text":"<pre><code>rmatmat(\n    x: NDArray[complexfloating[Any, Any]],\n) -&gt; npt.NDArray[np.complexfloating[Any, Any]]\n</code></pre> <p><code>A^H @ X</code> for a 2-D input of shape <code>(dim, k)</code> \u2014 SciPy <code>rmatmat</code>.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.QMatrixLinearOperator.rmatvec","title":"rmatvec  <code>method descriptor</code>","text":"<pre><code>rmatvec(\n    x: NDArray[complexfloating[Any, Any]],\n) -&gt; npt.NDArray[np.complexfloating[Any, Any]]\n</code></pre> <p><code>A^H @ x</code> for a 1-D input \u2014 SciPy <code>rmatvec</code>.  Computed as <code>conj(A^T @ conj(x))</code> so it matches the Hermitian adjoint even when the underlying matrix has complex entries.</p>"},{"location":"api/qmatrix/#expmop","title":"ExpmOp","text":""},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp","title":"ExpmOp","text":"<pre><code>ExpmOp(qop: QMatrixLinearOperator, a: complex)\n</code></pre> <p>Cached <code>exp(a \u00b7 A) \u00b7 v</code> action over a <code>QMatrixLinearOperator</code>.</p> <p>Construction runs the partitioned-Taylor parameter selection once.  Use <code>worker(...)</code> to obtain a worker that reuses scratch memory across <code>apply</code> calls.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp.__doc__","title":"__doc__  <code>class-attribute</code>","text":"<pre><code>__doc__ = \"Cached `exp(a \u00b7 A) \u00b7 v` action over a `QMatrixLinearOperator`.\\n\\nConstruction runs the partitioned-Taylor parameter selection once.  Use\\n`worker(...)` to obtain a worker that reuses scratch memory across\\n`apply` calls.\"\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp.__module__","title":"__module__  <code>class-attribute</code>","text":"<pre><code>__module__ = 'quspin_rs._rs'\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp.a","title":"a  <code>property</code>","text":"<pre><code>a: complex\n</code></pre>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp.dim","title":"dim  <code>property</code>","text":"<pre><code>dim: int\n</code></pre>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp.__new__","title":"__new__  <code>builtin</code>","text":"<pre><code>__new__(*args, **kwargs)\n</code></pre> <p>Create and return a new object.  See help(type) for accurate signature.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp.__repr__","title":"__repr__  <code>method descriptor</code>","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return repr(self).</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmOp.worker","title":"worker  <code>method descriptor</code>","text":"<pre><code>worker(\n    n_vec: int = 0, work: NDArray[Any] | None = None\n) -&gt; ExpmWorker | ExpmWorker2\n</code></pre> <p>Build a worker bound to this operator.</p> <p>Parameters:</p> Name Type Description Default <code>n_vec</code> <code>int</code> <p>Output dimensionality / batch capacity.  <code>0</code> (the default) returns a 1-D <code>ExpmWorker</code> for single-vector application; any value <code>&gt; 0</code> returns a 2-D <code>ExpmWorker2</code> whose <code>apply</code> accepts shape <code>(dim, k)</code> with <code>k &lt;= n_vec</code>.</p> <code>0</code> <code>work</code> <code>NDArray[Any] | None</code> <p>Optional caller-supplied scratch buffer adopted as the worker's backing storage (no copy).  For <code>n_vec == 0</code> a 1-D <code>complex128</code> array of length <code>&gt;= 2 * dim</code>; for <code>n_vec &gt; 0</code> a 2-D <code>complex128</code> array of shape <code>(&gt;= 2 * dim, &gt;= n_vec)</code>.  When <code>None</code> (default), a fresh buffer is allocated.</p> <code>None</code>"},{"location":"api/qmatrix/#expmworker","title":"ExpmWorker","text":""},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker","title":"ExpmWorker","text":"<p>1-D worker bound to an <code>ExpmOp</code>.  Holds <code>2 * dim</code> complex128 scratch.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker.__doc__","title":"__doc__  <code>class-attribute</code>","text":"<pre><code>__doc__ = \"1-D worker bound to an `ExpmOp`.  Holds `2 * dim` complex128 scratch.\"\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker.__module__","title":"__module__  <code>class-attribute</code>","text":"<pre><code>__module__ = 'quspin_rs._rs'\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker.dim","title":"dim  <code>property</code>","text":"<pre><code>dim: int\n</code></pre>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker.__repr__","title":"__repr__  <code>method descriptor</code>","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return repr(self).</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker.apply","title":"apply  <code>method descriptor</code>","text":"<pre><code>apply(f: NDArray[complexfloating[Any, Any]]) -&gt; None\n</code></pre> <p>Apply <code>f \u2190 exp(a \u00b7 A) \u00b7 f</code> in place.  <code>f</code> is a 1-D <code>complex128</code> array of length <code>dim</code>.</p> <p>Borrows <code>f</code> directly \u2014 no allocations on the apply hot path.</p>"},{"location":"api/qmatrix/#expmworker2","title":"ExpmWorker2","text":""},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker2","title":"ExpmWorker2","text":"<p>2-D batch worker bound to an <code>ExpmOp</code>.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker2.__doc__","title":"__doc__  <code>class-attribute</code>","text":"<pre><code>__doc__ = '2-D batch worker bound to an `ExpmOp`.'\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker2.__module__","title":"__module__  <code>class-attribute</code>","text":"<pre><code>__module__ = 'quspin_rs._rs'\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker2.dim","title":"dim  <code>property</code>","text":"<pre><code>dim: int\n</code></pre>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker2.n_vec","title":"n_vec  <code>property</code>","text":"<pre><code>n_vec: int\n</code></pre>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker2.__repr__","title":"__repr__  <code>method descriptor</code>","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return repr(self).</p>"},{"location":"api/qmatrix/#quspin_rs._rs.ExpmWorker2.apply","title":"apply  <code>method descriptor</code>","text":"<pre><code>apply(f: NDArray[complexfloating[Any, Any]]) -&gt; None\n</code></pre> <p>Apply <code>F \u2190 exp(a \u00b7 A) \u00b7 F</code> in place.  <code>F</code> must be a 2-D <code>complex128</code> array of shape <code>(dim, k)</code> with <code>k &lt;= n_vec</code>.</p> <p>Borrows <code>F</code> directly \u2014 no allocations on the apply hot path.</p>"},{"location":"api/qmatrix/#schrodingereq","title":"SchrodingerEq","text":""},{"location":"api/qmatrix/#quspin_rs._rs.SchrodingerEq","title":"SchrodingerEq","text":"<pre><code>SchrodingerEq(hamiltonian: Hamiltonian)\n</code></pre> <p>Schr\u00f6dinger equation integrator (Dopri5).</p> <p>Parameters:</p> Name Type Description Default <code>hamiltonian</code> <code>Hamiltonian</code> <p>A <code>Hamiltonian</code> to integrate.</p> required"},{"location":"api/qmatrix/#quspin_rs._rs.SchrodingerEq.integrate","title":"integrate","text":"<pre><code>integrate(\n    t0: float,\n    t_end: float,\n    y0: NDArray[Any],\n    rtol: float = 1e-10,\n    atol: float = 1e-12,\n) -&gt; npt.NDArray[Any]\n</code></pre> <p>Integrate to <code>t_end</code> and return the final state.</p> <p>Parameters:</p> Name Type Description Default <code>y0</code> <code>NDArray[Any]</code> <p>Initial state, shape <code>(dim,)</code>.</p> required <p>Returns:</p> Type Description <code>NDArray[Any]</code> <p>Final state, shape <code>(dim,)</code>.</p>"},{"location":"api/qmatrix/#quspin_rs._rs.SchrodingerEq.integrate_dense","title":"integrate_dense","text":"<pre><code>integrate_dense(\n    t0: float,\n    t_end: float,\n    y0: NDArray[Any],\n    rtol: float = 1e-10,\n    atol: float = 1e-12,\n) -&gt; tuple[npt.NDArray[np.float64], npt.NDArray[Any]]\n</code></pre> <p>Integrate and return all accepted time-step outputs.</p> <p>Returns:</p> Type Description <code>NDArray[float64]</code> <p><code>(times, states)</code> where <code>times</code> has shape <code>(n_steps,)</code> and</p> <code>NDArray[Any]</code> <p><code>states</code> has shape <code>(n_steps, dim)</code>.</p>"},{"location":"api/symmetry/","title":"Symmetry API","text":"<p>Pass a <code>SymmetryGroup</code> to <code>*Basis.symmetric(group, ham, seeds)</code>. See the guide for usage patterns.</p>"},{"location":"api/symmetry/#symmetrygroup","title":"SymmetryGroup","text":""},{"location":"api/symmetry/#quspin_rs.symmetry.SymmetryGroup","title":"SymmetryGroup","text":"<pre><code>SymmetryGroup(n_sites: int, lhss: int)\n</code></pre> <p>Collection of (element, character) pairs describing a symmetry group.</p> <p>Construct with <code>(n_sites, lhss)</code>, then add elements via :meth:<code>add</code>, :meth:<code>add_cyclic</code>, :meth:<code>close</code>, or :meth:<code>product</code>. Pass the result as the first positional argument to <code>*Basis.symmetric(group, ham, seeds)</code>.</p>"},{"location":"api/symmetry/#quspin_rs.symmetry.SymmetryGroup.add","title":"add","text":"<pre><code>add(element: 'SymElement', character: complex) -&gt; None\n</code></pre> <p>Add a single non-identity element with its 1-D-rep character.</p>"},{"location":"api/symmetry/#quspin_rs.symmetry.SymmetryGroup.add_cyclic","title":"add_cyclic","text":"<pre><code>add_cyclic(\n    generator: \"SymElement\",\n    *,\n    k: int | None = None,\n    eta: int | None = None,\n    char: complex | None = None\n) -&gt; None\n</code></pre> <p>Add g, g\u00b2, \u2026, g^(N-1) where N is the generator's computed order.</p> <p>Exactly one of <code>{k, eta, char}</code> must be supplied:</p> <ul> <li><code>k=int</code>    \u03c7(g^a) = exp(-2\u03c0i \u00b7 k \u00b7 a / N), any cyclic</li> <li><code>eta=\u00b11</code>   \u03c7(g^a) = \u03b7^a, requires N == 2</li> <li><code>char=z</code>   \u03c7(g^a) = z^a (user picks any consistent rep)</li> </ul> <p>Raises <code>ValueError</code> if more than one (or none) of <code>k</code> / <code>eta</code> / <code>char</code> is supplied, if <code>eta</code> is given on a non-order-2 generator, or if the generator is the identity (computed order less than 2).</p>"},{"location":"api/symmetry/#quspin_rs.symmetry.SymmetryGroup.close","title":"close","text":"<pre><code>close(\n    generators: list[\"SymElement\"],\n    char: Callable[[\"SymElement\"], complex],\n) -&gt; None\n</code></pre> <p>BFS-close the orbit under composition.</p> <p>For each generator and each element discovered so far, compose them and append any new (i.e. previously-unseen) action with the user-supplied character. The user is responsible for supplying a self-consistent 1-D representation; :meth:<code>validate</code> catches inconsistencies.</p> <p>A no-op when <code>generators</code> is empty.</p> <p>Compositions that produce the identity element are dropped (the identity is implicit in every group).</p>"},{"location":"api/symmetry/#quspin_rs.symmetry.SymmetryGroup.product","title":"product","text":"<pre><code>product(other: 'SymmetryGroup') -&gt; 'SymmetryGroup'\n</code></pre> <p>Out-of-place direct product. Both groups must share <code>(n_sites, lhss)</code>. Caller asserts the factors commute; :meth:<code>validate</code> catches non-commuting products at first build.</p> <p>The result contains every element of <code>self</code>, every element of <code>other</code>, plus every cross product <code>_compose(a, b)</code> with character <code>\u03c7_a \u00b7 \u03c7_b</code>. Compositions producing the identity are dropped (the identity is implicit in every group).</p> <p><code>self</code> and <code>other</code> are not mutated.</p>"},{"location":"api/symmetry/#quspin_rs.symmetry.SymmetryGroup.validate","title":"validate","text":"<pre><code>validate() -&gt; None\n</code></pre> <p>Eagerly run SymBasis::validate_group: closure under composition + 1-D-rep character consistency.</p> <p>Raises :class:<code>ValueError</code> if the supplied elements don't form a closed group, the characters violate <code>\u03c7(g\u00b7h) = \u03c7(g)\u00b7\u03c7(h)</code>, or any two elements have the same action.</p> <p><code>*Basis.symmetric(group, ...)</code> runs the same check implicitly on first build, so calling :meth:<code>validate</code> directly is opt-in early-feedback.</p>"},{"location":"api/symmetry/#element-constructors","title":"Element constructors","text":"<p>The three element constructors produce opaque <code>SymElement</code> handles that the <code>SymmetryGroup</code> consumes via <code>add</code> / <code>add_cyclic</code> / <code>close</code> / <code>product</code>.</p>"},{"location":"api/symmetry/#lattice","title":"Lattice","text":""},{"location":"api/symmetry/#quspin_rs._rs.Lattice","title":"Lattice  <code>builtin</code>","text":"<pre><code>Lattice(perm: list[int]) -&gt; SymElement\n</code></pre> <p>Pure-lattice symmetry element (site permutation only).</p> <p><code>perm[src] = dst</code> moves the dit at site <code>src</code> to site <code>dst</code>. <code>perm</code> must have length <code>n_sites</code> and contain a permutation of <code>0..n_sites</code> \u2014 validated downstream at *Basis.symmetric(...) time.</p>"},{"location":"api/symmetry/#local","title":"Local","text":""},{"location":"api/symmetry/#quspin_rs._rs.Local","title":"Local  <code>builtin</code>","text":"<pre><code>Local(\n    perm_vals: list[int], locs: list[int] | None = None\n) -&gt; SymElement\n</code></pre> <p>Pure-local symmetry element (per-site value-permutation only).</p> <p><code>perm_vals</code> is a permutation of <code>0..lhss</code> describing the per-site action; <code>locs</code> selects the sites the local op applies to (None \u2192 all sites). Validated downstream at *Basis.symmetric(...) time.</p>"},{"location":"api/symmetry/#composite","title":"Composite","text":""},{"location":"api/symmetry/#quspin_rs._rs.Composite","title":"Composite  <code>builtin</code>","text":"<pre><code>Composite(\n    perm: list[int],\n    perm_vals: list[int],\n    locs: list[int] | None = None,\n) -&gt; SymElement\n</code></pre> <p>Composite symmetry element (lattice + local applied atomically).</p> <p>Both <code>perm</code> (site permutation) and <code>perm_vals</code> (per-site value permutation) act as a single group element with one character. Use this for symmetries like PZ where neither component alone is a symmetry of the Hamiltonian.</p>"},{"location":"api/symmetry/#symelement","title":"SymElement","text":""},{"location":"api/symmetry/#quspin_rs._rs.SymElement","title":"SymElement","text":"<p>Opaque handle for a single symmetry-group element.</p> <p>Construct via the <code>Lattice</code>, <code>Local</code>, or <code>Composite</code> factory functions.  LHSS-agnostic \u2014 the actual lattice / local / composite decomposition is materialised when the element is added to a basis.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__module__","title":"__module__  <code>class-attribute</code>","text":"<pre><code>__module__ = 'quspin_rs._rs'\n</code></pre> <p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__eq__","title":"__eq__  <code>method descriptor</code>","text":"<pre><code>__eq__(value) -&gt; bool\n</code></pre> <p>Return self==value.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__ge__","title":"__ge__  <code>method descriptor</code>","text":"<pre><code>__ge__(value)\n</code></pre> <p>Return self&gt;=value.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__gt__","title":"__gt__  <code>method descriptor</code>","text":"<pre><code>__gt__(value)\n</code></pre> <p>Return self&gt;value.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__hash__","title":"__hash__  <code>method descriptor</code>","text":"<pre><code>__hash__() -&gt; int\n</code></pre> <p>Return hash(self).</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__le__","title":"__le__  <code>method descriptor</code>","text":"<pre><code>__le__(value)\n</code></pre> <p>Return self&lt;=value.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__lt__","title":"__lt__  <code>method descriptor</code>","text":"<pre><code>__lt__(value)\n</code></pre> <p>Return self&lt;value.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__ne__","title":"__ne__  <code>method descriptor</code>","text":"<pre><code>__ne__(value)\n</code></pre> <p>Return self!=value.</p>"},{"location":"api/symmetry/#quspin_rs._rs.SymElement.__repr__","title":"__repr__  <code>method descriptor</code>","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return repr(self).</p>"},{"location":"guide/petsc4py/","title":"Distributed assembly with petsc4py","text":"<p>For a Hamiltonian whose Hilbert space is too large to materialise on a single MPI rank, the standard pattern is \"each rank computes only its locally-owned rows and pushes them into a <code>PETSc.Mat</code>\". QuSpin supports this via <code>op.csr_slab(...)</code> \u2014 a row-range CSR materialisation that bypasses the global <code>QMatrix</code> entirely.</p>"},{"location":"guide/petsc4py/#the-api","title":"The API","text":"<pre><code>indptr, indices, data = op.csr_slab(\n    basis,                       # SpinBasis or FermionBasis\n    coeffs,                      # 1-D complex128, len == op.num_cindices\n    row_start: int,              # inclusive, 0-based\n    row_end: int,                # exclusive\n    dtype=np.complex128,         # output value dtype\n    drop_zeros=True,\n)\n</code></pre> <p>Returns three numpy arrays in the layout petsc4py's <code>Mat.setValuesCSR</code> / <code>Mat.createAIJ(csr=\u2026)</code> expect:</p> Array Shape Contents <code>indptr</code> <code>(row_end - row_start + 1,)</code> int64 Row pointer for the local slab; <code>indptr[0] == 0</code>, <code>indptr[-1] == nnz_local</code>. <code>indices</code> <code>(nnz_local,)</code> int64 Global column indices (zero-based). PETSc handles the diag/off-diag split internally. <code>data</code> <code>(nnz_local,)</code> <code>dtype</code> Accumulated <code>\u03a3_c coeffs[c] * stored_value</code> per entry \u2014 same semantics as <code>QMatrix.to_csr(coeffs)</code>. <p>Each rank only ever holds its <code>(rend - rstart)</code> rows of CSR plus a small per-row scratch buffer \u2014 memory is bounded by the slab, regardless of the global dimension.</p> <p>V1 covers <code>PauliOperator</code> paired with <code>SpinBasis</code> or <code>FermionBasis</code>. Other operator families (<code>Bond</code>, <code>Boson</code>, <code>Fermion</code>, <code>Monomial</code>) follow in mechanical follow-up PRs.</p>"},{"location":"guide/petsc4py/#minimal-petsc4py-integration","title":"Minimal petsc4py integration","text":"<pre><code>from mpi4py import MPI\nfrom petsc4py import PETSc\nimport numpy as np\nfrom quspin_rs._rs import PauliOperator, SpinBasis\n\nn_sites = 12\nop = PauliOperator(\n    [(\"XX\", [[1.0, i, i + 1] for i in range(n_sites - 1)])],\n    [(\"ZZ\", [[1.0, i, i + 1] for i in range(n_sites - 1)])],\n)\nbasis = SpinBasis.full(n_sites)\n\n# Let PETSc decide the row partition.\nmat = PETSc.Mat().create(comm=MPI.COMM_WORLD)\nmat.setSizes(((PETSc.DECIDE, basis.size), (basis.size, basis.size)))\nmat.setType(PETSc.Mat.Type.AIJ)\nmat.setUp()\nrstart, rend = mat.getOwnershipRange()\n\n# Each rank materialises just its local rows.\ncoeffs = np.array([1.0 + 0j, 1.0 + 0j], dtype=np.complex128)\nindptr, indices, data = op.csr_slab(\n    basis, coeffs, rstart, rend, dtype=np.dtype(\"complex128\"),\n)\nmat.setValuesCSR(\n    indptr.astype(PETSc.IntType, copy=False),\n    indices.astype(PETSc.IntType, copy=False),\n    data,\n)\nmat.assemble()\n</code></pre> <p><code>indptr</code> and <code>indices</code> are emitted as <code>int64</code>; the <code>astype(PETSc.IntType, copy=False)</code> is a no-op on 64-bit PETSc builds and a cheap convert on 32-bit builds.</p>"},{"location":"guide/petsc4py/#choosing-the-value-dtype","title":"Choosing the value dtype","text":"<p>PETSc is built with either real (<code>float64</code>) or complex (<code>complex128</code>) scalars at compile time, exposed via <code>PETSc.ScalarType</code>. Pass that dtype straight through to <code>csr_slab</code> when your Hamiltonian's matrix elements fit it:</p> <pre><code># Real Hamiltonian (XX, ZZ, real Heisenberg, \u2026) on a real PETSc build:\ndata = op.csr_slab(basis, coeffs, rstart, rend, dtype=np.dtype(\"float64\"))[2]\n\n# Hamiltonian with non-trivial imaginary entries (complex hopping,\n# magnetic flux, \u2026) \u2014 needs PETSc with --with-scalar-type=complex:\ndata = op.csr_slab(basis, coeffs, rstart, rend, dtype=np.dtype(\"complex128\"))[2]\n</code></pre> <p>Internally the kernel always accumulates in <code>complex128</code> so the operator-coefficient machinery stays uniform. Requesting <code>dtype=float64</code> casts on the wrapper boundary; numpy emits a <code>ComplexWarning</code> because the cast can in principle drop imaginary parts. Suppress it at the call site when you know the matrix is real:</p> <pre><code>import warnings\nwith warnings.catch_warnings():\n    warnings.simplefilter(\"ignore\", np.exceptions.ComplexWarning)\n    indptr, indices, data = op.csr_slab(\n        basis, coeffs, rstart, rend, dtype=np.dtype(\"float64\"),\n    )\n</code></pre> <p>If the matrix has non-trivial imaginary entries and PETSc was built with real scalars, the cast silently loses them \u2014 assert the imaginary part is small before casting if you're not sure.</p>"},{"location":"guide/petsc4py/#time-dependent-hamiltonians","title":"Time-dependent Hamiltonians","text":"<p><code>csr_slab</code> lives on the operator, not on <code>Hamiltonian</code> \u2014 for a time-dependent build, construct one PETSc <code>Mat</code> per cindex (the operator terms whose coefficients evolve independently) by calling <code>csr_slab</code> once per term with a coefficient mask that selects only that cindex, then combine in PETSc:</p> <pre><code>mats = []\nfor c in range(op.num_cindices):\n    mask = np.zeros(op.num_cindices, dtype=np.complex128)\n    mask[c] = 1.0\n    indptr, indices, data = op.csr_slab(basis, mask, rstart, rend, dtype=...)\n    m = PETSc.Mat().createAIJ(...).setValuesCSR(indptr.astype(...), indices.astype(...), data)\n    m.assemble()\n    mats.append(m)\n\n# Per timestep:\nH_t = mats[0].copy()\nH_t.zeroEntries()\nfor c, m in enumerate(mats):\n    H_t.axpy(coeff_c(t), m)\n</code></pre> <p>This keeps the slab construction one-shot per cindex, with only the PETSc <code>axpy</code> per timestep.</p>"},{"location":"guide/petsc4py/#runnable-demo","title":"Runnable demo","text":"<p>A minimal proof of concept lives at <code>examples/petsc4py_chunked_build.py</code> in the source tree. It builds the same Hamiltonian two ways \u2014 distributed via <code>csr_slab</code> and reference via <code>QMatrix.to_csr</code> on rank 0 \u2014 and asserts the assembled matrices match. Runs both serial and <code>mpirun -n 4</code>.</p> <p>See <code>examples/README.md</code> for installing the optional <code>petsc4py</code> dependency group and notes on matching <code>petsc4py</code> to your system PETSc version.</p>"},{"location":"guide/symmetry/","title":"Symmetry groups","text":"<p><code>SymmetryGroup</code> is the user-facing handle for building the symmetry group you pass to <code>*Basis.symmetric(...)</code>. Construct elements via <code>Lattice</code>, <code>Local</code>, and <code>Composite</code>; collect them via <code>add</code> / <code>add_cyclic</code> / <code>close</code> / <code>product</code>; then hand the group to a basis constructor.</p> <pre><code>basis = SpinBasis.symmetric(group, ham, seeds)\n</code></pre> <p><code>n_sites</code> and <code>lhss</code> come from the group; <code>FermionBasis</code> rejects <code>group.lhss != 2</code> with a <code>TypeError</code>.</p>"},{"location":"guide/symmetry/#element-constructors","title":"Element constructors","text":"Constructor Use it for <code>Lattice(perm)</code> site permutation only \u2014 translation, parity, reflection <code>Local(perm_vals, locs=None)</code> per-site value-permutation only \u2014 spin-flip, dit-permutation <code>Composite(perm, perm_vals, locs=None)</code> the two acting as a single tied element (e.g. <code>PZ</code>) <p>Each returns an opaque <code>SymElement</code> handle.</p>"},{"location":"guide/symmetry/#common-patterns","title":"Common patterns","text":""},{"location":"guide/symmetry/#cyclic-translation-parity-spin-flip","title":"Cyclic \u2014 translation, parity, spin-flip","text":"<pre><code>from quspin_rs import SymmetryGroup, Lattice, Local\n\n# Z_L translation, momentum k.\ngroup = SymmetryGroup(n_sites=4, lhss=2)\ngroup.add_cyclic(Lattice([1, 2, 3, 0]), k=1)\n</code></pre> <p><code>add_cyclic(generator, k=\u2026)</code> enumerates <code>g, g\u00b2, \u2026, g^(N-1)</code> automatically and attaches the matching characters <code>\u03c7(g\u1d43) = exp(-2\u03c0i \u00b7 k \u00b7 a / N)</code>. The cyclic order <code>N</code> is computed from the generator.</p> <p>For <code>order = 2</code> factors (parity, spin-flip) the kwarg <code>eta=\u00b11</code> is sugar:</p> <pre><code>group.add_cyclic(Local([1, 0]), eta=-1)   # parity-odd Z\u2082 sector\n</code></pre> <p>For arbitrary 1-D characters, use <code>char=z</code>:</p> <pre><code>group.add_cyclic(Local([1, 0]), char=-1.0 + 0j)   # same as eta=-1\n</code></pre>"},{"location":"guide/symmetry/#direct-product-abelian-factor-groups","title":"Direct product \u2014 abelian factor groups","text":"<p>Chain <code>add_cyclic</code> calls don't Cartesian-product. To combine commuting factor groups, build each separately and call <code>.product(other)</code>:</p> <pre><code>T = SymmetryGroup(n_sites=4, lhss=2)\nT.add_cyclic(Lattice([1, 2, 3, 0]), k=0)\n\nZ = SymmetryGroup(n_sites=4, lhss=2)\nZ.add_cyclic(Local([1, 0]), eta=-1)\n\ngroup = T.product(Z)   # Z_4 \u00d7 Z_2\n</code></pre> <p><code>product</code> is out-of-place \u2014 <code>T</code> and <code>Z</code> stay reusable. The factors must commute; <code>validate_group</code> (run automatically at build time) catches non-commuting products.</p>"},{"location":"guide/symmetry/#composite-generators-pz-particle-hole","title":"Composite generators \u2014 PZ, particle-hole, \u2026","text":"<p>When a single tied generator (<code>PZ</code>, particle-hole, etc.) is the symmetry but neither <code>P</code> nor <code>Z</code> alone is:</p> <pre><code>PZ = Composite(perm=[3, 2, 1, 0], perm_vals=[1, 0])\ngroup = SymmetryGroup(n_sites=4, lhss=2)\ngroup.add_cyclic(PZ, eta=-1)\n</code></pre> <p>The <code>perm</code> and <code>perm_vals</code> components are applied atomically as one element with a single character.</p>"},{"location":"guide/symmetry/#non-abelian-dihedral-point-groups","title":"Non-abelian \u2014 dihedral, point groups","text":"<p>Use <code>close</code> with a user-supplied 1-D character function. The walker BFS-closes the orbit under composition:</p> <pre><code>T = Lattice([1, 2, 3, 0])\nP = Lattice([3, 2, 1, 0])\n\ngroup = SymmetryGroup(n_sites=4, lhss=2)\ngroup.close(generators=[T, P], char=lambda elem: 1.0)   # trivial rep on D_4\n</code></pre> <p>For non-trivial 1-D reps the <code>char(elem)</code> callable distinguishes elements by their action \u2014 see <code>SymBasis::validate_group</code> for the checks the closure must satisfy.</p>"},{"location":"guide/symmetry/#validating-early","title":"Validating early","text":"<pre><code>group.validate()   # opt-in: closure + 1-D character consistency\n</code></pre> <p><code>*Basis.symmetric(...)</code> runs the same check implicitly on first build, so calling <code>validate()</code> directly is just for early feedback when iterating on group construction.</p>"},{"location":"guide/symmetry/#compatibility-check","title":"Compatibility check","text":"<p><code>*Basis.symmetric(group, ham, seeds)</code> enforces:</p> Basis Required of <code>group</code> <code>SpinBasis</code> <code>group.lhss</code> becomes the basis LHSS <code>FermionBasis</code> <code>group.lhss == 2</code> (TypeError otherwise); <code>fermionic = True</code> set internally <code>BosonBasis</code> <code>group.lhss</code> becomes the basis LHSS <code>GenericBasis</code> <code>group.lhss</code> becomes the basis LHSS <p><code>group.n_sites</code> always becomes the basis <code>n_sites</code>; there is no separate keyword.</p>"},{"location":"guide/symmetry/#see-also","title":"See also","text":"<ul> <li><code>scripts/symmetry_group_demo.py</code> \u2014 runnable walkthrough of the patterns above.</li> <li>Symmetry API reference \u2014 full method listings.</li> <li>Spec: <code>docs/superpowers/specs/2026-04-26-python-symmetry-group-api-design.md</code>.</li> </ul>"}]}